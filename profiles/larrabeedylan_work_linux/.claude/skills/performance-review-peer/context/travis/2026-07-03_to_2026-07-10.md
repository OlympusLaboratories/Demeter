# Travis Thompson: 2026-07-03 to 2026-07-10

## Peer's Contributions (from Dylan's perspective)
Travis was the most frequent collaborator this week and acts as a senior/lead on platform infra. Observed contributions:
- Driving migration of the CI build cluster to a GKE standard cluster for better control over node behavior; has C3 machine-type fallback enabled in the compute classes to mitigate stockouts.
- Owns/knows the Buildkite and ArgoCD infrastructure deeply; proposed longer-term direction (move ArgoCD templates to Helm charts in tlaloc, per-transformer vram/vcpu via Dataflow for FTL 2.0).
- Set up a GitHub API key for rate-limiting and flagged the need to mirror the kubeconform artifact to remove an external dependency.
- Created an Access Requests Notion page to start documenting onboarding/access processes.
- Framing FTL 2.0 architecture direction (isolation, resource requirements per transformer).

## Shared Slack Threads
- **Master CI "agent lost" flakiness (platform-infra-team):** Dylan raised and investigated; Travis confirmed the Google capacity crunch, noted his GKE-standard migration and C3 fallback, and agreed auto-retry on agent-lost should be increased.
- **kubeconform GitHub rate-limiting (eng-build):** Dylan triaged and root-caused; Travis explained he'd set up a GitHub API key and agreed the artifact should be mirrored; offered to look at it alongside another lint fix. Dylan offered to pair.
- **PLAT-1974 COP submission OOM (alerts-platform-infra):** Travis praised Dylan's find ("good find"), speculated the memory jump came from py3.12 + proto upgrade, and asked Dylan to open a follow-up ticket for CrashLoopBackOff alerting.
- **PLAT-1891 / NOAA NAM OOM (prod):** Travis pulled Dylan in ("fyi @Dylan") and discussed per-transformer resource customization in the new FTL/Dataflow architecture.
- **ArgoCD stuck sync (platform-infra):** Travis asked Dylan why the ApplicationSet retry limit was set to -1; Dylan explained the single-MR manifest+terraform rationale, which Travis accepted.
- **Ivy GridOps onboarding (platform-infra-team):** Travis (while Dylan on-call) asked Dylan to onboard Ivy via Firebase, created the Access Requests doc page.

## MR Interactions
- No direct MR review exchanges captured this week, though several of Travis's areas (Buildkite config, tlaloc-env compute classes) overlap Dylan's work.

## Shared Linear Tickets
- PLAT-1974 (Travis requested the crashloopbackoff follow-up), PLAT-1891, PLAT-1978 (Travis requested), PLAT-1964 (build flakiness).

## Collaboration Summary
Travis and Dylan operated as close platform-infra partners this week: Travis sets architectural direction and holds infra ownership, while Dylan does hands-on root-causing and incident response and serves as a knowledge source (e.g., ArgoCD retry semantics). Travis repeatedly delegated follow-ups to Dylan and endorsed his incident work, and they aligned on longer-term fixes (artifact mirroring, GKE-standard migration, FTL 2.0 resource isolation).
