# Austin Park: 2026-07-03 to 2026-07-10

## Peer's Contributions (from Dylan's perspective)
- Root-caused the CAISO/NOAA NAM data gap in detail: a NAM 12z run (85 gribs) landed on nodes where transformer containers request only 4Gi but were consuming 13–17Gi, overpacking nodes, driving memory to zero, and triggering mass pod eviction; hour f29 was collateral (redelivered onto evicting pods 5× until Pub/Sub abandoned it).
- Backfilled the missing data via a manual transformer run and verified by sampling the CAISO storage schedulers.
- Recommended increasing the transform memory request and constraining deployments by limits; offered CAISO OASIS as a good robustness test target for FTL work.

## Shared Slack Threads
- **PLAT-1891 / CAISO NAM OOM (prod):** Austin did the forensic root-cause and backfill; Dylan picked up the platform-side follow-up (per-rule compute specs, PLAT-1891) and how the new FTL/Dataflow architecture should handle this. Austin proactively suggested CAISO OASIS as a test target for Dylan's robustness work.

## MR Interactions
- None directly captured this week.

## Shared Linear Tickets
- PLAT-1891 (created from the thread Austin root-caused); Austin is also a reviewer on the Caballero telemetry BQ cost MR (!13418) in Mark's initiative.

## Collaboration Summary
Austin provided the deep operational diagnosis and immediate remediation (backfill) for the NAM data gap; Dylan took the systemic follow-through into FTL 2.0. Austin also gave Dylan a concrete, helpful pointer (CAISO OASIS) for testing pipeline robustness, indicating a collaborative, forward-looking working relationship on data reliability.
