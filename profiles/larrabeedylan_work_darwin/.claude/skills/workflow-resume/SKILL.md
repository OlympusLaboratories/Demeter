# Workflow Resume — Restart a Killed Swarm from Cache

Bring a dead swarm back. When a `Workflow`-based skill — `review-code`, `changes-description`, `review-kludge` — is cut off by a spend/usage limit, a `TaskStop`, or a CLI exit, every agent that finished before the cut is still on disk. This skill finds that run, says what it lost and why, and then either **resumes** it (replaying the cached agents, running only what is missing) or **harvests** the finished results and completes the job without a new fleet.

I need you to restart the workfow started in this session from cache, re-using as much of the work that the swarm has already done as possible. It stopped at before it was finished so there is still pleanty of left to review so analysing the resutls at this point is not appropriate. I'm worried that restarting workflows takes a ton of tokens and it will chew up my quota again before it is finished reviewing again, so avoid that - if I wanted to throw away all the work that the swarm has already done and chew up a bunch of tokens I woul just restart the whole workflow from scratch. IMPORTANT: DO NOT DO ANYTHING ELSE BESIDES RESTART THE WORKFLOW FROM CAHHE AND THEN REPORT THE FINDINGS.
