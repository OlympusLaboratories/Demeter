# Global Dippy config
allow terraform *
allow ruff check *
allow ruff format *
allow gcloud storage ls *
allow python3 -m pytest *
allow python -m pytest *
allow pytest *
allow git checkout *
allow mkdir *
allow npm run build *
allow npm run lint *
